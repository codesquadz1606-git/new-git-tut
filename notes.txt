Class 1 : Introduction To github
git init : empty repoistory
git remote add origin "repo-name"
git add filename
git commit -m "message"
git push origin main

git log : All comits dikh jaye.
git clone "repo-name" : To copy the whole repository to the new folder

Class 2 : Branches , CI-CD piplines
git branch : Give list of Branches
git branch <branch-name> : Create the new branch
git checkout <branch-name> : Switch the branch
git merge <branch-name> : To merge with the master branch

git checkout -b <branch-name> : Created new branch & switched to new branch

git branch -d <branch-name> : For deleting any branch.
git branch -D <branch-name> : Force delete
